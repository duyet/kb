# `topics/npm/`

## Concepts

- [npx looks up a scoped package's last path segment as the executable](tech-npx-scoped-package-bin.md) — npx @scope/name runs a bin named `name`. Multiple bins without that name fail with "not found" / could not determine executable.
