# Inbox — 2026-08-18

- 02:05 — anyrouter donate pool: req_5c719c367ea646bc9a2ce3af routed google/gemini-3.7-flash via openrouter-pool at $0; BYOK 0 pricing != OpenRouter free; adding donate coverage free vs paid (feat/pool-donate-coverage)
- 02:07 — anyrouter: openrouter-pool upstream_cost_usd is $0 because BYOK catalog is 0/0 and we do not request OpenRouter usage.include; gemini-3.7-flash list ~0.375/1.875 per 1M; feat/pool-upstream-cost
- 16:17 — Standing: after chmonitor merges, verify main CI tip is green; keep Insights/schema-diff queries off the hot path (no extra sequential system.tables+parts on every collectStorage).
- 11:08 — duyetbot codes via Grok Build on Herdr; public accounts github.com/duyetbot and x.com/duyetbot; weekday chmonitor issue triage
- 11:37 — AnyRouter StepFun Step Plan API returns HTTP 400 `you have no active step plan subscription` (type `request_params_invalid`) when the key has no Step Plan; treat as quota-exhausted 400 so routing falls through, and classify the error-spike as external quota rather than unknown/platform_outage

- 20:37 — chmonitor/chmonitor does not have the Cursor GitHub app; Cursor cloud agents cannot launch. Use Grok Build on a local checkout / worktrees. Squash-merge (never release-please).

- 20:49 — AnyRouter health JSON is status + components (api, providers, smoke_tests, workflows, routing), not the old ok/uptime/providers[] envelope. Docs still describe the old shape.
- 20:49 — anyrouter/free requires a Go plan or a donated key; there is no no-key daily allowance.
- 20:49 — AnyRouter dashboard chrome on main is a 5-item rail (Overview, Keys, Logs, Routing, BYOK) with Credits on the user chip; not live until deploy.
- 21:00 — news app: article permalinks are /ai/{id}; landing feed is client-fetched from /api/feed; sitemap.xml is not shipped.
- 14:20 — news.duyet.net live bugs: Worker-only /sitemap.xml + robots Sitemap; homepage must run_worker_first or the static shell has no stories; article share tags need name=description + twitter + canonical; title_vi missing → honest EN fallback not invented VI; Stripe/OpenRouter dups were URL-hash + LLM-only merge (code bug). PR https://github.com/duyet/monorepo/pull/1301 — not merged, not deployed.
- 16:00 — Debian 13 apt only ships Node 20; Wrangler 4 needs >=22. Installed official Node 22.23.2 LTS to /usr/local and dpkg-diverted /usr/bin/node so 22 is the system default. Debian 20 remains at /usr/bin/node.distrib.

- 00:05 — AnyRouter dashboard 5-item rail (Overview, Keys, Logs, Routing, BYOK) is live on the signed-in shell; Credits lives on the user chip (PR 2273).
- 00:05 — Public marketing SSR still dual-renders EN plus hidden VI `data-lang` spans (fail-closed CSS). Homepage first HTML is ~1.1MB with ~359KB inlined CSS.
