# Inbox — 2026-09-11

- 13:53 — AnyRouter error-monitor can POST structured JSON to an optional Grok Bot webhook (GROK_BOT_OUTAGE_WEBHOOK_URL) alongside Telegram; Bearer sender key; unset URL is no-op.
- 14:35 — aidr.today public brand files: PNG logos 200 via Worker ASSETS; SVG/JPG GET 404 when ASSETS SPA-falls back to HTML. handlePublicAsset should fetch a clean GET Accept:*/* and sniff SVG bodies so /favicon.svg is image/svg+xml.
