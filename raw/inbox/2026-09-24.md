- 18:19 — anyrouter-dev/cli: the gh OAuth token lacks `workflow` scope, so HTTPS pushes touching .github/workflows are rejected and Git Data API trees containing workflow files 404; push via the sshorigin SSH remote instead. `ar` in PATH is shadowed by a non-binutils tool — cargo builds need AR=/usr/bin/ar.
