- 02:05 — oma CI: test job failures on unrelated PRs share one signature: `[cold-start-flush] failed: D1_ERROR: no such table: sessions` spam in workers-pool suite — environmental (missing D1 migration in test bindings for some worker), not per-PR. Repo policy: typecheck is the only blocking gate (ci.yml comment re SessionDO deadlock).
- 02:05 — oma: pre-assigning D1 migration numbers to parallel agents (0024→stripe idempotency, 0025→turn debit) per earlier collision lesson; two 0021_* and two 0023_* pairs already shipped on main, wrangler applies filename order.
- 02:06 — oma goal run: 6 worktree agents on issues #159/160/161/162/163/181/182 + docs batch (#173,188-192,195); merged pre-existing PRs #202 #203 (typecheck green, test fail = known signature).
- 02:26 — oma: D1 batch statements can't branch on earlier results — race-proof idempotent credit = standalone INSERT OR IGNORE first, check meta.changes, then batch the dependent writes (PR #207 applyEntry pattern).
- 02:26 — oma: sessions already carry metadata.scheduled_run.schedule_id — counting live sessions per schedule needs json_extract (SQLite-only) not a new column (PR #209).
- 02:26 — subagent race-test lesson: an HTTP-level Promise.all "concurrency" test can serialize through crypto/signature hops and never race; verify a race test fails with the fix reverted before trusting it (stripe-fix agent did this).
- 03:03 — this env: SSH port 22 to github.com unreachable; push over HTTPS explicit URL (`git push https://github.com/duyet/oma.git <branch>`) — gh credential helper auths it. Tell spawned agents up front.
- 03:03 — GitHub Actions can permanently refuse to create workflow runs for one PR branch (webhook drop during a merge burst?) while CodeRabbit/Socket etc. run fine; close/reopen AND synchronize pushes didn't help. Resolution: produce the gate locally (merge origin/main into branch → pnpm typecheck + targeted tests) and squash-merge; "Base branch was modified" error = retry once.
- 03:04 — oma .env.example had shipped a REAL GitHub OAuth client secret + CF account/zone ids (now stripped, PR #219); values live in git history → user must rotate. SHA-256 denylist guard added to main-node boot (known-insecure-secrets.ts).
- 04:03 — oma latent bug pattern: cf-route-services buildRouteServices() silently omitted `environments` from the RouteServices bundle — validation code downstream no-opped in prod. When adding a service to packages/http-routes factories, verify BOTH runtimes' wiring (cf-route-services.ts AND main-node) actually forward it.
- 04:03 — vitest root-level tests can't vi.mock packages hoisted only into a sub-app's node_modules (e.g. @ai-sdk/mcp in apps/agent) — workaround: fake the wire protocol behind the fetch binding instead (precedent: test/unit/mcp-parallel-discovery.test.ts).
- 04:04 — subagent killed mid-turn by Anthropic API 5xx: SendMessage to the same agent id resumes it with its transcript intact — cheaper than respawn; worktree survives.
- 04:45 — INCIDENT: oma prod 503 for ~20min. PR #239's boot-secret gate fail-closed on leaked .env.example placeholders — and prod genuinely still runs FIVE of them (PLATFORM_ROOT_SECRET, BETTER_AUTH_SECRET, API_KEY, INTEGRATIONS_INTERNAL_SECRET, GITHUB_CLIENT_SECRET). Hotfix PR #243: missing→503, leaked→serve+log-once. Rotation tracked oma#242 (GITHUB_CLIENT_SECRET needs OAuth app UI; PLATFORM_ROOT_SECRET rotation breaks stored-credential decryption). Lessons: (1) a fail-closed guard against a condition PROD ITSELF exhibits = self-inflicted outage — check prod's actual state before shipping enforcement; (2) deploy workflows must assert /health post-deploy (oma#244); (3) typecheck-only merge bars let behavior changes ship unverified — /health was 503 while the deploy workflow reported success.
- 05:00 — oma autonomous run COMPLETE: 39 issues closed, 25 PRs merged (14 parallel worktree agents + 1 resumed after API-kill + 1 main-session hotfix), 18 issues filed (7 self-fixed in-run), 7 triage comments, prod 503 incident self-inflicted + self-resolved in ~25min. Test suite went 142 baseline failures → 2 (PR #241) — CI test gate (#164) now actually unblockable.
- 07:52 — OMA autonomous issue run: closed #184 #185 (verified shipped in PR #240); commented operator-blocker on #242 + filed #256 (root-secret re-encryption tool); babysitting PR #255 (4 bug fixes) + PR #250 (Node public surface, conflicts); dispatched #135 turn-watchdog to codex
